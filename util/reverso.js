
/*
const languages = ['de', 'fr', 'es', 'en', 'he', 'it', 'nl', 'pl', 'pt', 'ro', 'ru', 'ar', 'ja'];

const compare = {
    'fr': 'french',
    'en': 'english',
    'ar': 'arabic',
    'de': 'german',
    'es': 'spanish',
    'he': 'hebrew',
    'it': 'italian',
    'ja': 'japanese',
    '': ''
};

const link = {
    synonyms (lang, word) {
        return `https://synonyms.reverso.net/synonym/${lang}/${word}`;
    },
    context (from, to, sentence) {

    },
    autocomplete (lang, letters) {
        return `https://synonyms.reverso.net/api/v2/autocomplete/${lang}/?term=${encodeURIComponent(letters)}`;
    }
};*/